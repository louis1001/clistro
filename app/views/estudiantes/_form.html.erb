<%= form_with(model: estudiante, local: true) do |form| %>
  <% if estudiante.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(estudiante.errors.count, "error") %> prohibited this estudiante from being saved:</h2>

      <ul>
      <% estudiante.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :carnet %>
    <br/>
    <%= form.text_field :carnet, placeholder: "AAAA-NNNNL", size: 11, maxlength: 10 %>
  </div>

  <div class="field">
    <%= form.label :nombre %>
    <br/>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :apellido %>
    <br/>
    <%= form.text_field :apellido %>
  </div>

  <div class="field">
    <%= form.label :carrera_id %>
    <br/>
    <%= form.select('carrera_id', options_from_collection_for_select(@carreras, :first, :last)) %>
  </div>

  <div class="field">
    <%= form.label :grupo_id %>
    <br/>
    <%= form.select('grupo_id', options_from_collection_for_select(@grupos, :first, :last)) %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
